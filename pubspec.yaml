name: attendance_app
publish_to: "none"
description: Cross-platform attendance app with MySQL backend
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  dio: ^5.6.0
  intl: ^0.20.2
  shared_preferences: ^2.2.3
  url_launcher: ^6.3.0
  path_provider: ^2.1.4
  pdf: ^3.11.0
  printing: ^5.13.3
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  open_filex: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true